var React = require('react');

var Timer = React.createClass({
  render: function () {
    return (
      <div>
        <p>This is the Timer component!</p>
      </div>
    )
  }
})

module.exports = Timer;
